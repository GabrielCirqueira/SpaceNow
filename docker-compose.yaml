services:
  database:
    image: mysql:8.3
    container_name: spacenow_database
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: spacenow
      MYSQL_USER: spacenow
      MYSQL_PASSWORD: spacenow
    ports:
      - "${DATABASE_HOST_PORT:-3309}:${DATABASE_CONTAINER_PORT:-3306}"
    volumes:
      - db_data:/var/lib/mysql

  symfony:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spacenow_symfony
    restart: always
    env_file:
      - ports.env
    ports:
      - "${BACKEND_PORT:-8000}:80"
      - "${SUPERVISOR_PORT:-9001}:9001"
    volumes:
      - .:/var/www/html
    depends_on:
      - database
      # - redis

  vite-react:
    build:
      context: .
      dockerfile: docker/vite.Dockerfile
    container_name: spacenow_vite_react
    working_dir: /app
    user: "${DEV_UID:-1000}:${DEV_GID:-1000}"
    volumes:
      - .:/app
    command: sh -c "npm install --legacy-peer-deps && npm run dev"
    env_file:
      - ports.env
    ports:
      - "${FRONTEND_PORT:-5173}:${FRONTEND_PORT:-5173}"
    depends_on:
      - symfony

  # redis:
  #   image: redis:7-alpine
  #   container_name: redis
  #   restart: always
  #   command: redis-server --appendonly yes
  #   volumes:
  #     - redis_data:/data
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 10

  # redisinsight:
  #   image: redis/redisinsight:latest
  #   container_name: redisinsight
  #   restart: always
  #   ports:
  #     - "5540:5540"
  #   depends_on:
  #     - redis

volumes:
  db_data:
  # redis_data:
