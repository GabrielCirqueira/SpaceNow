# Port configuration shared across Docker services and tooling.
# Adjust values here to change exposed ports globally.

# Symfony HTTP server exposed by the backend container (host and container port).
# 8000
BACKEND_PORT=1010

# Supervisor control panel exposed by the backend container.
# 9001
SUPERVISOR_PORT=1011

# Vite development server exposed by the frontend container (host and container port).
# 5173
FRONTEND_PORT=1012

# MySQL port published on the host (container port remains 3306).
# 3309
DATABASE_HOST_PORT=1013
## If you ever change the container port, update this value as well.
# 3306
DATABASE_CONTAINER_PORT=3306
